import React from 'react';
const init = {}

const AuthReducer = (state = init , action) => {
   switch (action.type) {
    case 'LOGIN':
        
        break;
   
    default:
        break;
   }
}

export default AuthReducer;
